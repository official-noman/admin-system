version: '3.8'
services:
  web:
    build: .
    ports: ["8000:8000"]
    volumes: [".:/app"]
    depends_on: [redis]
    environment:
      - DISPLAY=host.docker.internal:0.0  
    extra_hosts:
      - "host.docker.internal:host-gateway"
  redis:
    image: redis:alpine
    ports: ["6379:6379"]